{
  "name": "react-custom-question",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel ./runtime/components-dev.js --hmr-hostname localhost --https",
    "build": "rm -rf ./dist && parcel build ./runtime/components.js"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}